package com.project.password.manager.services;

import org.springframework.stereotype.Service;

@Service
public class AuditService {
    // log(Long userId, AuditAction action, Map<String, Object> details)
    // getAuditLogs(Long userId, Pageable pageable)
    // getRecentActivity(Long userId, int days)
}
