package com.project.password.manager.services;

import org.springframework.stereotype.Service;

@Service
public class PasswordGeneratorService {
    // generatePassword(GeneratePasswordRequest) -> String
    // Options: length, uppercase, lowercase, numbers, symbols
    // Ensure cryptographically secure random
    // generatePassphrase(int wordCount) -> String
}