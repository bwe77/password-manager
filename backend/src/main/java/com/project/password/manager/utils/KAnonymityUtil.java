package com.project.password.manager.utils;

public class KAnonymityUtil {
    // hashPasswordForBreachCheck(String password) -> String (SHA-1)
    // getHashPrefix(String hash) -> String (first 5 chars)
    // compareHashSuffix(String hash, Set<String> breachedSuffixes) -> boolean
}
