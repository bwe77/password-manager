package com.project.password.manager.services;

import org.springframework.stereotype.Service;

@Service
public class PasswordSharingService {
    // sharePassword(Long userId, Long entryId, SharePasswordRequest)
    // getSharedPassword(String shareToken) -> PasswordDetailResponse
    // revokeShare(Long userId, Long shareId)
    // trackAccess(Long shareId)
}
